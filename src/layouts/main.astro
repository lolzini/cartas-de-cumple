---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<main
  class:list={[
    "flex min-h-[calc(100vh-3.5rem)] items-center justify-center",
    className,
  ]}
>
  <slot />
</main>
